const ScholarSpaceToolbar = [
    // Text style
    [{ 'header': [1, 2, 3, false] }],

    // Inline formatting
    ['bold', 'italic', 'underline', 'strike'],

    // Text colour and background
    [{ 'color': [] }, { 'background': [] }],

    // Lists and indents
    [{ 'list': 'ordered' }, { 'list': 'bullet' }],
    [{ 'indent': '-1' }, { 'indent': '+1' }],

    // Alignment
    [{ 'align': [] }],

    // Block elements
    ['blockquote', 'code-block'],

    // Links
    ['link'],

    // Clear formatting
    ['clean']
];
